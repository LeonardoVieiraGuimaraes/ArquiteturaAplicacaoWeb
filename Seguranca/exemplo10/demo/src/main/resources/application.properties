# Configuração do Authorization Server (ajuste a porta se necessário)
server.port=8080

# Configuração do JWT (exemplo de senha simétrica, para produção use JWK)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080
spring.security.oauth2.authorizationserver.issuer-url=http://localhost:8080

# Outras propriedades do seu projeto...